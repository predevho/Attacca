# TODO-READY

바로 작업 가능한 작업 목록.

---

(없음. BACKLOG를 참고하세요.)

* [ ] 공지 3건 등록(운영) — 어드민(`admin`)으로 `/admin/notices` 에서 등록한다. 문안은 `scripts/seed/data.json` 의 `notices`.
  * 종류는 셋 다 `공지(NOTICE)`, 일시·장소는 비운다. **1번만 상단 고정** — 홈 히어로가 고정 공지를 태운다.
  * 승격 직후에는 **재로그인**해야 ADMIN 권한이 토큰에 반영된다(access 30분).
  * 달력을 채우려면 **일시가 있는 공지**가 필요하다. 실제 예정된 일이 생기면 종류를 `일정(EVENT)`으로 두고 일시를 넣는다 — 없는 일정을 지어내지 않는다.

