# TODO-DOING

현재 진행 중인 작업.

---

* **외부 공연(KOPIS)·입시 공지 반입(IMPORT)** — Task 1~11 구현과 Task 12 문서·검증 완료. FE 493/493·lint 오류 0건·build 성공, BE JDK 21 `cleanTest test` `BUILD SUCCESSFUL`, `git diff --check` 통과.
  * 남은 운영 조치: KOPIS 인증키(`KOPIS_SERVICE_KEY`) 발급, User-Agent 연락처(`IMPORT_CONTACT`) 결정, 실제 KOPIS 호출·대학 게시판 첫 페이지·승인/포스터 흐름 smoke test.
